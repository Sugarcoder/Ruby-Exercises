break = halt something (do a loop, and then take a break from it at some point)

# Example: 

loop do
  programmer.study
  break if programmer.job?
end





# times.do = Doing something a number of times

# Example: 

def sweets(candy)
  5.times do
  candy.eat
  end
end
