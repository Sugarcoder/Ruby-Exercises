# return = using "return" exits the method (avoid using it...or use it at the very end).




# "add_two" is a method, and 2 is the integer


# Example: 


def add_two(integer)
  return integer + 2
end

puts add_two(2)





# * = Splat operator

# The splat operator is used to handle methods that has a variable parameter list.



# Examples:


def add(*numbers) 
  numbers.inject(0) { |sum, number| sum + number}
end

puts add(1)
puts add((1, 2)
puts (1, 2, 3)
 

    
    
    
def introduction(age, gender, *names)

  "Meet #{names.join(' ')}, who is #{age} and #{gender}"
  
end
    
    
    
