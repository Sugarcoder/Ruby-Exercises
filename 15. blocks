# blocks = cannot be stored in a variable and is not an object


# yield (keyword) = can call a single lambda (that has been passed to a method) without using a parameter list. 


# Example:

def this_block(number)
  yield (number)
end

puts this_block(1) { |number| number + 1}




def (a,b)
  yield (a,b)
end
